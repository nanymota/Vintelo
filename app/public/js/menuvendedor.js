// Configurações de autenticação e tipo de usuário para vendedor
// Estas variáveis são definidas pelo servidor e usadas pelo navegacao.js

// Função para voltar na navegação
function goBack() {
    window.history.back();
}